{% import "_base/_macros.j2" as macros -%}
{{ macros.comment("Network configuration for bridge " ~ bridge.name ~ " on " ~ node.name) }}
[Match]
Name={{ bridge.name }}

[Network]
LinkLocalAddressing=no
